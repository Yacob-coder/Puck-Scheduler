import { PageHeader } from '@/components/layout/PageHeader'

export function Slots() {
  return (
    <PageHeader
      title="Ice slots"
      subtitle="Enter the slots your rink has allocated — we handle the rest."
    />
  )
}
